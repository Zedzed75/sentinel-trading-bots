# Bot runtime state (balance references, Kelly history, locks)
sentinel_state.json
alpha_state.json
trend_state.json
orchestrator_state.json
risk_scale.json

# Telegram secrets (token) and notifier state (chat_id)
telegram_config.json
telegram_state.json

# Web dashboard secrets (Basic Auth)
dashboard_config.json

# Bot 7: Anthropic API key (secret), weather and runtime state
macro_config.json
macro_weather.json
macro_state.json
macro_history.json
macro_signal.json

# Bot 8: arbitrage database, state and dashboard summary
arbitrage.db
arbitrage_state.json
arbitrage_summary.json

# Runtime logs
logs/

# Python
__pycache__/
*.pyc
.venv/
