# Etat runtime des bots (references de balance, historique Kelly, verrous)
sentinel_state.json
alpha_state.json
trend_state.json
orchestrator_state.json
risk_scale.json

# Secrets Telegram (token) et etat du notifier (chat_id)
telegram_config.json
telegram_state.json

# Secrets du dashboard web (Basic Auth)
dashboard_config.json

# Bot 7 : cle API Anthropic (secret), meteo et etat runtime
macro_config.json
macro_weather.json
macro_state.json

# Logs runtime
logs/

# Python
__pycache__/
*.pyc
.venv/
